{
  "name": "calendar-booking-api-contract",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSpec contract for the calendar booking application",
  "scripts": {
    "compile": "tsp compile .",
    "check": "tsp compile . --no-emit"
  },
  "devDependencies": {
    "@typespec/compiler": "1.15.0",
    "@typespec/http": "1.15.0",
    "@typespec/openapi": "1.15.0",
    "@typespec/openapi3": "1.15.0"
  }
}
