kind: project
entrypoint: main.tsp

emit:
  - "@typespec/openapi3"

options:
  "@typespec/openapi3":
    emitter-output-dir: "{project-root}/openapi"
    output-file: "openapi.yaml"
    file-type: yaml
    openapi-versions:
      - "3.1.0"
    omit-unreachable-types: false
